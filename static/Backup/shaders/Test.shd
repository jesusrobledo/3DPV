attributes:
  0:
    name: 'position'
    size: 3
    type: 'float'

uniforms:
  0:
    name: 'color'
    type: 'vec3'

vertexShader: |
              #version 300 es
              in vec3 position;
              void main() {
                gl_Position = vec4(position, 1.0);
                gl_PointSize = 1.0;
              }

fragmentShader: |
                #version 300 es
                precision highp float;
                uniform vec3 color;
                out vec4 outColor;
                void main() {
                  outColor = vec4(color.x, color.y, color.z, 1.0);
                }